# CLIPEncoder

The CLIP encoder embeds images with a pretrained
[open_clip](https://github.com/mlfoundations/open_clip) model instead of
aggregating local descriptors. It requires the `nn` extra
(`pip install "pyvisim[nn]"`).

## API reference

```{eval-rst}
.. autoclass:: pyvisim.encoders.CLIPEncoder
   :members:
   :inherited-members:
   :show-inheritance:
```
